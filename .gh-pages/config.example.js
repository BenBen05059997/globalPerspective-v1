// Example runtime config for the REST proxy (DO NOT COMMIT real values).
// Copy to public/config.js and set your API Gateway invoke URL locally.
// Note: public/config.js is ignored by git.

window.SENSITIVE_PROXY_ENDPOINT = 'https://CHANGE_ME.execute-api.YOUR-REGION.amazonaws.com/prod/proxy';
