# archive/kickstarter/

Artifacts from the **abandoned Kickstarter direction** (the project pivoted away from a crowdfunding/consumer launch to the current free-content + Polar-membership model, then to the enterprise/analyst direction — see `project-docs/strategy/ANALYST_TOOL_DIRECTION.md`).

Kept for history; **none of this is wired into the live app.**

| File | What it is |
|---|---|
| `Project-budget-template.xlsx` | Kickstarter's blank budget template (unfilled boilerplate; accidentally committed 2026-04-20). Renamed from a mojibake filename. |
| `KickstarterBanner.jsx` / `.css` | A frontend banner component — orphaned (no importers) since the campaign was dropped; relocated out of `frontend/src/components/` so it isn't mistaken for live code. |
