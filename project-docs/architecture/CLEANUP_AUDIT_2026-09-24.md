# Cleanup Audit — 2026-09-24

_Purpose: while Phase 4 (severity prompt fix) is blocked on the provider charge, sweep the whole
system for **no-longer-used weight** — dead code, retired-but-still-present Lambdas, orphaned
schedules, stale data, unused deps — verify each candidate against code + live AWS, then decide
keep / archive / delete. **This doc is the checklist + verified findings. Nothing is deleted in
this pass.**_

Method: checklist written by monitor (Fable) → 3 Sonnet verification agents (read-only) → 2-agent
keep-vs-cut debate → consolidated plan. Every claim below must carry a `file:line` or a live AWS
command output reference — no "probably dead."

---

## §0 Guardrails — NEVER flag these for deletion (verify they're intact instead)

- `pair_analyses_list` DDB records + whatever serves them — pair *pages* are deleted but this
  **powers /map arcs** (memory: project_pair_intelligence).
- `docs/config.js` — operator-owned runtime config.
- Frozen quality artifacts: `quality/severity_gold_set.json`, `quality/calibration/severity-2026-09-14.md`,
  `quality/calibration/live-score-flags-2026-09-15.md`, `quality/prompts/dimensions_v2_*`.
- R3 tier-matcher code behind `ENABLE_R3_TIER` — dormant by design, clock restarts after charge.
- Anything where **deployed zip ≠ repo source** (`newsAnalyze` carries parked credits code in repo;
  Signal API + breaking-scorer bytes drifted historically). Repo-side "unused" ≠ safe to delete
  when the deployed artifact is the truth — zip-diff before judging.
- `docs/` build output (regenerated by deploy.sh; only `config.js` is hand-managed).
- Superseded-but-bannered docs (IMPORTANCE_SCALE_MAPPING, DEBATE, FRAGMENTS, DESIGN_UPGRADES) —
  kept deliberately as evidence trail per the 2026-09-15 governance pass.
- Plaintext Lambda env secret store — do not propose Secrets Manager (standing feedback).

---

## §1 Verification checklist

### A. Frontend (global-perspectives-starter/frontend/src/)
- [ ] A1. Orphan routes: `/contact`, `/spider-demo` — still orphaned? Anything else unreachable
      from nav/links? (crawl the route table vs actual `<Link>`/`navigate` usage)
- [ ] A2. Components never imported (or imported only by dead components) — full import-graph pass.
- [ ] A3. Hooks/utils never imported.
- [ ] A4. CSS files / classes belonging only to deleted pages (e.g. pair pages remnants).
- [ ] A5. Dead feature flags / config branches that can never fire.
- [ ] A6. `npm ls`-level: dependencies in package.json nothing imports.
- [ ] A7. Root `src/` legacy Amplify scaffold — confirm nothing references it; size it.

### B. Backend Lambdas + schedules + env (live AWS read-only allowed)
- [ ] B1. Full inventory: repo `amplify/backend/function/*` vs `aws lambda list-functions` —
      functions in repo but not deployed, deployed but not in repo, or both-but-drifted.
- [ ] B2. EventBridge/scheduler rules: DISABLED rules (candidates: drift email cron, InvokeLinkedIn
      after the 09-11 retirement), rules targeting deleted/retired functions, rules that error.
- [ ] B3. linkedInAutoPost retirement completeness: function gone? schedule gone? repo dir still
      present? `significance` field's only consumer was `newsPostLinkedin` sort — is THAT lambda
      the retired one or a separate live one? (This decides whether `significance` is now
      write-only dead data.)
- [ ] B4. Lambdas whose output nothing reads: for each writer, name its DDB PK/S3 prefix and its
      consumer (frontend hook, other lambda, email). Writers with zero consumers = flag.
- [ ] B5. Env-var hygiene: `deepseek-chat` alias stragglers; GROK_*/XAI naming pointing elsewhere;
      env vars the code never reads (per-function grep).
- [ ] B6. CloudWatch log groups / last-invocation ages — functions not invoked in 30+ days that
      hold enabled schedules (broken cron?) or no schedule at all (manually dead?).
- [ ] B7. SNS topics/subscriptions with zero subscribers or pointing at dead functions.

### C. Repo / scripts / docs / quality
- [ ] C1. `scripts/` — each script: still runnable? referenced by any playbook/doc? one-off that
      already served its purpose?
- [ ] C2. `quality/` — one-off eval scripts vs living tools; stale generated reports
      (dashboard.md, calibration/latest.md are known-stale but regenerate-don't-delete).
- [ ] C3. `project-docs/` plans marked DONE/CANCELLED whose content is fully absorbed elsewhere —
      archive candidates (move, never rewrite). Cross-check INDEX.md status column truthfulness.
- [ ] C4. Dead top-level files (old mockups, .html scratch, exports, zips, logs accidentally
      committed).
- [ ] C5. `agent-kit/` — anything referencing files that no longer exist.
- [ ] C6. Git-tracked build artifacts outside `docs/` (dist/ leaks, .map files, node_modules leaks).

### D. Data stores (read-only)
- [ ] D1. DDB `SummarizeAndPredict` PK prefixes actually present vs prefixes any code reads —
      orphaned prefix families (written by retired lambdas, or read by deleted pages).
- [ ] D2. S3 world bucket: zero-byte corpus files from the outage window (count + size), prefixes
      nothing reads, `signals/` vs live signal API.
- [ ] D3. Other DDB tables (UserPrefs, saved items, client errors, signal tables) — any table with
      no live reader/writer.

---

## §2 Verified findings

_(filled by verification agents — every entry: what, evidence, current status)_

### §2.A Frontend (verified 2026-09-24)

**ALIVE surprises (checklist assumptions corrected):**
- **`/contact` is NOT an orphan** — global footer link, `Layout.jsx:219`. Checklist A1 was wrong.
- `driver.js` dep, `SituationMap3D.jsx`, `atoms/AnalysisVisuals.jsx`, `onboarding/tour-theme.css`
  all alive via dynamic/`.jsx`-suffixed imports — naive grep would misreport them. Any future
  automated dead-code scan must handle `lazy()`/`import()` and extension-suffixed specifiers.
- `window.POLAR_CREDIT_PACKS` empty-fallback is designed behavior for the parked credits feature
  (restProxy.js:387-390), not a dead branch.
- `/__boom` is a deliberate unlinked diagnostic route (App.jsx:40) — keep.

**Dead, evidence-backed (zero importers / zero inbound links):**
1. Routes: **`/map-legacy`** (sole reference is its own route line) and **`/spider-demo`**
   (self-documented throwaway, SpiderDemo.jsx:1-2 includes its own deletion recipe).
2. Components (0 importers): `ApiKeyGate.jsx`, `BriefingCard.jsx` (428 ln), `CountryGrouping.jsx`
   (377 ln), `PerspectiveComparison.jsx` (338 ln), `SectionNav.jsx`, `SideNav.jsx`,
   `WorldMap.jsx` (797 ln, pre-V2 map), `LoadingStates.jsx` (359 ln, skeletons barrel),
   `MiniMap.jsx`.
3. Transitively dead: `ArticleCard.jsx`, `MapSidePanel.jsx` (only importers are the dead files
   above); hooks `usePrediction.js`, `useSummary.js` (only consumer = dead ArticleCard; the
   similarly named `PredictionDisplay.jsx`/`SummaryDisplay.jsx` components are ALIVE — distinct).
4. Hooks/utils/services (0 refs): `useBookmarks.js`, `useResearchBriefing.js`, `useTraceCause.js`,
   `utils/geocoding.js`, `services/appsyncProxy.js` (with its `APPSYNC_*` window flags).
5. CSS: `SummaryDisplay.css` (0 refs; the alive component imports AIComponents.css instead),
   `WorldMap.css` (transitive). No dead pair-page CSS exists.
6. Deps: `aws-amplify`, `@aws-amplify/api-graphql` — zero imports in src/, vite config, index.html.
7. Root `/src/` legacy Amplify scaffold: 7 files, 88K, zero external references (only .gitignore
   name-matches).

**⚠ Monitor addendum — the `/map-legacy` ↔ pair-arcs entanglement (operator decision needed):**
`/map` now renders `SituationHome`; **`/map-legacy` is the ONLY route mounting `WorldMapV2`**
(App.jsx:102-103), and `WorldMapV2.jsx:165` is the ONLY consumer of `usePairAnalyses`. So the
pair-arcs surface — whose backend chain (weekly `newsPairIntelligence` cron → `pair_analyses_list`)
is a §0 keeper and verified alive in §2.B — is currently user-reachable only via an unlinked URL.
Three test files also exercise WorldMapV2 (layers/searchBar/signalFilters tests). Options:
(a) keep `/map-legacy` as-is (data has a hidden surface), (b) re-link it from nav, (c) retire
WorldMapV2 + its tests + the pair frontend chain AND the weekly backend cron together as one
coherent decision. Cutting the route alone while the cron keeps producing unviewable data would be
the worst outcome. → §3 debate + operator.

### §2.B Backend / AWS (verified 2026-09-24, read-only pass)

**ALIVE surprises (hypotheses corrected):**
- **`significance` is NOT dead data.** The live weekly poster `newsPostLinkedin` (ENABLED schedule
  `InvokeLinkedIn`) still reads it: `newsPostLinkedIn/src/index.js:51` (`SIGNIFICANCE_ORDER`),
  `:171-172` (sort), `:272`. The retired lambda was the *other* one (`linkedInAutoPost`). The B3
  hypothesis in §1 is wrong — significance stays a consumer-backed field.
- **Pair-intelligence chain verified intact end-to-end:** `newsPairIntelligence` → DDB `PAIR#`/
  `PAIR_ANALYSIS` → `newsSensitiveData` `pair_analyses_list` action (index.js:921-960) →
  `restProxy.js:187` → `usePairAnalyses.js`; weekly cron ENABLED. §0 guardrail holds.
- `newsGdacsIngest` + `newsPredictionsSnapshot`: fresh logs 09-23, crons ENABLED — alive.
- GlobalPerspectiveAlerts (singular) SNS topic alive, 1 email subscriber, referenced by 4 lambdas'
  `SNS_TOPIC_ARN`.

**Cleanup candidates (dead, evidence-backed):**
1. **`amplify/backend/function/linkedInAutoPost/`** — deployed function DELETED from AWS
   (`ResourceNotFoundException`), no schedule references it, and the repo dir's `src/` holds only
   `node_modules/` + a stale `deploy.zip` (index.js already removed 09-11). Pure cruft shell.
2. **`amplify/backend/function/newsStripeWebhook/`** — repo-only, never found deployed under any
   name variant among the account's 93 functions; Stripe path superseded by Polar. Scaffold that
   never shipped.
3. **SNS topic `GlobalPerspectivesAlerts` (plural — near-duplicate name)** — zero matches in any
   lambda source; 2 subscribers (email + AWS Chatbot endpoint). Likely console-created for a
   CloudWatch-alarm/Chatbot wiring outside the repo's surface. **NEEDS-OPERATOR check in the
   CloudWatch/Chatbot console before touching.**
4. **`newsAnalyze-sandbox` env: `GROK_MODEL=deepseek-chat`** — the retired model alias, literal
   straggler. Function itself is dormant-by-design sandbox (last log 06-29), so low urgency.
5. Naming/hygiene (not deletions): deployed-name drift `newsPostLinkedIn`(repo)↔`newsPostLinkedin`
   (deployed), `-dev` suffixes on newsInvokeGemini / newsSensitiveData / NewsProjectInvokeAgent;
   `XAI_API_KEY`/`GROK_*` vars actually holding DeepSeek keys (~11 fns) or Gemini keys
   (newsInvokeGemini-dev, newsEconomicQuality); unused `XAI_API_KEY_BACKUP` (real xAI-format key,
   never primary) across many fns; only `newsModelGuard` uses honest `DEEPSEEK_*` names.

**Dormant by design (NOT dead — do not cut):**
- DISABLED rules `TriggerBreakingEmailSend` + `TriggerDriftEmailSend` → both are paused *modes* of
  the alive `newsEmailSender` (weekly mode ENABLED). Matches recorded decisions (0 subscribers).
  Detection side (`TriggerBreakingAlert` → `newsBreakingAlert`, every 4h) is live and separate.
- `newsAnalyze-sandbox` / `newsPolarBilling-sandbox` — parked sandboxes, no crons, idle since
  ~06-29/07-02 per CloudWatch. Parked credits feature (POLAR_BILLING_PLAN §5).

**Declared gaps (would need a follow-up pass):** exhaustive writer→reader map for all ~37 lambdas
(only the flagged ones traced); per-function unused-env-var grep (only the model-key axis done;
`newsInvokeGemini-dev` carries an `OPENAI_API_KEY` worth a grep); CloudWatch staleness only
spot-checked (4 fns). Raw JSON in session scratchpad.

### §2.C/D Repo, docs, data stores (verified 2026-09-24, read-only pass)

**Clean bills of health:**
- `scripts/` (root): all 13 alive or documented-KEEP; git hooks path verified active
  (`core.hooksPath=.githooks`, pre-push exists). `setup-cloudwatch-alarms.sh`/`setup-dynamodb-gsi.sh`
  = one-offs kept as infra-as-code (their GSI `SK-generatedAt-index` verified live).
- `quality/`: no orphans; the severity/eval/dashboard/verify chains all cross-referenced.
- `project-docs/`: **no true orphan docs** — 124 files all trace to INDEX.md directly or as
  sub-evidence of indexed parents (EVENT_REGISTRY phase-results family). Minor INDEX granularity
  gap only. Spot-checked status claims accurate (drift cron DISABLED confirmed live).
- `agent-kit/`: no broken references (`WORKING_QUEUE.md` is a convention name, not a file).
- No tracked dist/, *.map, or node_modules leaks. `.DS_Store`, the zip, and the **Firebase admin
  SDK key at repo root are on disk but NOT git-tracked** (gitignored — no leak; operator should
  know the key file sits in the working tree).
- DDB: every PK prefix family sampled (2,854-item table, exhaustive scan) has a live reader.
  Correction to §1.D1: drift data lives as `SK: DRIFT#<date>` under COUNTRY#/THREAD# PKs, not a
  PK prefix — not orphaned, different key scheme. All 11 Global-Perspectives DDB tables alive
  (some via env-var indirection, verified against deployed config). No orphan tables.
- S3 `predictions/track_record.json`, `threads/story-map.json`, `signals/` (421MB, Signal API
  store) — all alive, single-object full-replace patterns are by design.

**Cleanup candidates (dead, evidence-backed):**
1. Repo-root one-offs (README.md:24 already calls them one-offs): `test-gemini.js` (**confirmed
   broken** — syntax error line 3), `simple-prompt.js` (references undefined `limit`), root
   `index.html` (legacy meta-refresh stub superseded by custom domain).
2. **`global-perspectives-starter/get-pip.py`** — 2.1MB pip bootstrap script, tracked in git,
   unrelated to the app. Largest tracked file in the repo.
3. S3 `tags/backfill/2026-09-11.json` (245KB) — documented as matcher input
   (EVENT_REGISTRY_PLAN.md:95) but **zero readers in any lambda source**. Dead write.
4. S3 `shadow/` (6 objects, all 2026-09-08) — T3c shadow-test residual; measurement closed per
   MAP_HOME_SITUATION_LEDGER. Archive/delete candidate.
5. Root `package-lock.json` (101-byte empty stub) — harmless, trivial delete.

**Known-issue confirmations (NOT new incidents — do not re-escalate):**
- Zero-byte hourly corpus files: 263/388 objects, ongoing through 09-23 — this is the known
  DeepSeek balance outage (since 09-13) starving `newsSituationIngest`'s classifier. Resolves
  when the operator charges the account. (`corpus/gdelt/` + `corpus/gdacs/` keyless paths still
  writing real content.) Decision needed later: delete the ~263 zero-byte files after recovery.
- `audit/` latest.json files 11-12 days stale — same outage family; re-check after charge before
  calling the audit lambdas broken.

## §3 Debate summary — keep vs cut (Cutter vs Keeper, adjudicated 2026-09-24)

The two agents **agreed** on most of the inventory: linkedInAutoPost dir, root one-offs,
S3 shadow/, WorldMap.jsx (pre-V2), the frontend dead-component sweep, root /src/ scaffold, the
sandbox env fix, SNS-needs-operator, naming-drift-leave-alone, corpus-files-wait-for-charge.
Three genuine disputes, resolved by monitor:

1. **`newsStripeWebhook` dir** — Cutter: prose in ARCHITECTURE.md is the reference, code adds
   nothing. Keeper: ARCHITECTURE.md:507 records a *deliberate* 2026-06-01 keep-for-reference
   decision; overturning a recorded decision needs an explicit operator yes, not a default-to-
   delete. **Resolution: Keeper — NEEDS-OPERATOR.** Low stakes, but recorded decisions don't get
   silently reversed by a cleanup pass.

2. **S3 `tags/backfill/2026-09-11.json`** — Cutter (and the §2.C verifier) called it a dead write
   (zero readers today). Keeper re-read EVENT_REGISTRY_PLAN.md: it is the **pre-staged launch-day
   input for the dormant R3 matcher** ("full launch-day coverage instead of forward-only"), i.e.
   the same workstream as the §0-guardrailed ENABLE_R3_TIER code whose clock restarts after the
   charge. Zero-readers-today is expected for a pre-stage. Deleting it would force redoing a
   3-day tagging pass. **Resolution: Keeper wins decisively — KEEP; delete only if the operator
   ever abandons R3.** (Lesson recorded: "zero readers" is not "dead" when the reader is dormant
   by design.)

3. **The map entanglement (11)** — Cutter proposed "B-prime": port the pair-arcs layer into
   SituationHome, then delete WorldMapV2 + `/map-legacy` + retarget its 3 tests; strongest point:
   WorldMapV2 is **eagerly imported** (App.jsx:23, verified twice) so 797 lines of d3 ship in the
   main bundle on every pageload while being unreachable by nav. Keeper verified the port is
   **new construction, not relocation** — SituationMap3D has deck.gl `ArcLayer` available but
   zero pair wiring; the 3 test files `render(<WorldMapV2/>)` directly (deleting the component
   breaks them at import, "retarget" = rewrite); and a feature build must not ride inside a
   cleanup commit past the operator's deploy gate. **Resolution: Keeper on sequencing, Cutter's
   bundle finding preserved as evidence — WorldMapV2/`/map-legacy`/tests are untouched pending
   the operator's three-way call (§4 D3); WorldMap.jsx (pre-V2) is cut now; the eager-import cost
   is listed as a fact for the operator to weigh.** If the operator picks "keep unlinked," the
   cheap harm-reduction is a one-line `lazy()` conversion of the import (still a src change →
   rides the next normal deploy, not a special one).

## §4 Consolidated cleanup plan (nothing executed yet — awaiting operator go)

### Tier A — SAFE-DELETE, no build/deploy needed (backend + repo-root + S3 + env)
| # | Action | Evidence | Execution status |
|---|---|---|---|
| A1 | `git rm -r amplify/backend/function/linkedInAutoPost/` (23MB shell: node_modules + stale zip) **+ fix ARCHITECTURE.md:481** (falsely says dir already removed) same commit | §2.B, both debaters | **EXECUTED 2026-09-24** — dir was entirely git-ignored/untracked (node_modules + `amplify/**/*.zip`), so removed via plain `rm -rf` rather than `git rm` (nothing was in the index); ARCHITECTURE.md:481 corrected. |
| A2 | `git rm test-gemini.js simple-prompt.js index.html package-lock.json` (repo root) + `git rm global-perspectives-starter/get-pip.py` (2.1MB) + update README.md:24 one-off list | §2.C; test-gemini.js provably broken | **EXECUTED 2026-09-24** — all 5 files removed via `git rm`; README.md:24 updated. |
| A3 | `aws s3 rm --recursive s3://globalperspective-world-280362093938/shadow/` (6 objects, T3c closed per ledger) | §2.C | **EXECUTED 2026-09-24** — confirmed exactly 6 objects listed, then deleted. |
| A4 | `newsAnalyze-sandbox`: env `GROK_MODEL` deepseek-chat → deepseek-v4-pro (merge-not-clobber) | §2.B | **EXECUTED 2026-09-24** — fetched full env, merged only `GROK_MODEL`, wrote back via `update-function-configuration`; verified live value now `deepseek-v4-pro`. |

### Tier B — SAFE-DELETE, requires `npm run build` verify (deploy rides the NEXT gated deploy, never auto-pushed)
| # | Action | Evidence | Execution status |
|---|---|---|---|
| B1 | Frontend sweep: rm ApiKeyGate, BriefingCard, CountryGrouping, PerspectiveComparison, SectionNav, SideNav, LoadingStates, MiniMap, ArticleCard, MapSidePanel (.jsx); hooks usePrediction, useSummary, useBookmarks, useResearchBriefing, useTraceCause; utils/geocoding.js; services/appsyncProxy.js; SummaryDisplay.css; **WorldMap.jsx + WorldMap.css** (pre-V2, zero importers). **Same commit: strike ARCHITECTURE.md rows 1270 / 1310 / 1318-1320 / 1324 + WorldMap rows in map section.** Build must pass; browser click-through before any deploy per standing feedback. | §2.A | **EXECUTED 2026-09-24** — re-grepped every filename for importers before deletion (all zero outside the deletion set); all 20 files removed via `git rm`; `npm run build` passed clean, `npx vitest run` passed 235/235 (19 files, incl. the 3 WorldMapV2 tests — untouched); ARCHITECTURE.md rows/prose struck. Browser click-through / deploy deferred — not run, per instruction (deploy rides the next gated deploy). |
| B2 | `git rm -r src/` (root legacy Amplify scaffold, 88K, zero refs) | §2.A/§2.C | **EXECUTED 2026-09-24** — 5 tracked files removed via `git rm -r src/`; 2 git-ignored files (`aws-exports.js`, `amplifyconfiguration.json`) went with the directory. |
| B3 | `npm uninstall aws-amplify @aws-amplify/api-graphql` (frontend) — check `npm ls` for transitive reintroduction + sanity-read the lockfile diff + clean build | §2.A; Keeper caveat | **EXECUTED 2026-09-24** — `npm ls aws-amplify` empty (no transitive reintroduction); `package.json` diff is removal-only (2 lines); build clean afterward. |

### Tier C — CUT-LATER, explicit gates
| # | Action | Gate |
|---|---|---|
| C1 | Delete ~263 zero-byte `corpus/YYYY/MM/DD/HH.jsonl` objects (pre-stage the key list now) | DeepSeek charge lands AND classifier writes real content again |
| C2 | S3 `tags/backfill/2026-09-11.json` | ONLY if operator abandons R3 matcher — otherwise KEEP (it's R3's launch-day input) |

### Tier D — NEEDS-OPERATOR-DECISION (do not touch)
| # | Question | Options | Execution status |
|---|---|---|---|
| D1 | `newsStripeWebhook/` repo dir — 2026-06-01 recorded "keep for reference"; still wanted? | keep (default) / delete | **EXECUTED 2026-09-24** — operator explicitly reversed the 2026-06-01 keep-for-reference decision; `git rm -r amplify/backend/function/newsStripeWebhook` executed, ARCHITECTURE.md #12 updated (git-recoverable from history). |
| D2 | SNS topic `GlobalPerspectivesAlerts` (plural) — check AWS Chatbot/CloudWatch console: live alarm wiring? | **RESOLVED 2026-09-24: KEEP — it is ALIVE.** Live AWS check found ~14 CloudWatch alarms (Lambda-Errors-Spike, lambda-errors-\*, dynamodb throttle events, lambda-throttles-\*) targeting it → Chatbot + email; created by `scripts/setup-cloudwatch-alarms.sh`. Not an orphan — the audit grep missed it because it's wired at the CloudWatch layer, not in Lambda source. Documented in ARCHITECTURE.md alerting section. |
| D3 | Pair-arcs surface: `/map-legacy` is the only route to WorldMapV2, sole consumer of pair data; weekly cron produces data almost nobody can find; WorldMapV2 also eagerly ships in the main bundle | **RESOLVED 2026-09-24 in two steps:** operator first chose (c) port-then-remove; the port was built + render/toggle-verified, then operator cancelled it ("no build" — the new map is the design, harvest ideas instead). Port archived on branch `archive/pair-arcs-port`, code reverted from main; ideas → `redesign-ux/_reference/LEGACY_MAP_IDEA_HARVEST_2026-09-24.md`. **Legacy map removal proceeds as pure cleanup.** OPEN REMAINDER: the weekly `TriggerPairIntelligenceWeekly` cron will produce data with no display surface — operator to decide disable vs leave warm. **Legacy map REMOVED 2026-09-24** — `WorldMapV2.jsx`/`.css`, the `/map-legacy` route (App.jsx), `usePairAnalyses.js`, and the 3 tests that rendered it (`signalFilters`/`searchBar`/`layers`.test.jsx) deleted via `git rm`; stale WorldMapV2 comments tidied in `tokens.js`/`utils/threadPath.js`/`utils/topicMatch.js`/`macroValues.test.js`; `npm run build` clean and `npx vitest run` green (16 files / 193 tests, down from 19/235 — exactly the 3 deleted files' tests). SpiderDemo untouched; backend pair pipeline (`newsPairIntelligence`, cron, `pair_analyses_list`) untouched per scope. Deploy deferred to the next explicit gated `./deploy.sh`. |

### Tier E — KEEP, confirmed alive (record so nobody re-flags them)
`/contact` (footer-linked) · `significance` field (live newsPostLinkedin sort) · pair chain
end-to-end · driver.js + all dynamic-import files · `/__boom` diagnostic · DISABLED email rules
(dormant by design) · sandboxes (parked) · naming drift (accepted risk, not worth live renames) ·
`XAI_API_KEY_BACKUP` vars · all scripts/, quality/, project-docs/, DDB tables & prefixes ·
`predictions/track_record.json`, `threads/story-map.json`, `signals/` S3 stores.

**Execution note:** Tier A is one commit; Tier B is one commit (build-verified, deploy deferred to
the next explicitly-approved deploy); C/D fire on their gates. Estimated total: ~25MB repo weight,
~2,800 lines dead frontend code, 2 deps, 2 repo dirs, ~270 S3 objects.
