# archive/design-mockups/

Standalone HTML **design mockups** used as visual references during earlier redesigns. Not shipped, not generated, not wired to the app — kept for history.

| File | What it is |
|---|---|
| `Brief.html` | The editorial "Direction A" mockup the Home v2 redesign was built to match (see `Home.css` header comment + `project-docs/redesign-ux/_shipped/REDESIGN_PLAN.md`). |
| `daily-brief-template.html` | The standalone style mockup the daily-brief layout was ported from (noted in `CHANGES.md`). |

Both were previously untracked at the repo root; moved here 2026-09-08 to declutter root.
