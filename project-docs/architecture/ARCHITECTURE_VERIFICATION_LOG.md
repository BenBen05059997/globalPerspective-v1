# Architecture verification log

Reverse-chronological, append-only. This is an audit/verification history for
`ARCHITECTURE.md`, not a change log (that's `CHANGES.md`). Moved out of `ARCHITECTURE.md`
line 5 on 2026-09-24 (see `project-docs/architecture/CONTEXT_HYGIENE_DECISIONS_2026-09-24.md`)
because a single 26KB line of history was consuming the read budget of every "read
ARCHITECTURE.md first" pass.

---

**Last verified:** 2026-08-01 (refresh: **WORKTREE CONSOLIDATION — `signal-api` MERGED, P6 PERK SURFACING SHIPPED, `/map` ECONOMY LENS REMOVED**. A survey of the 21 parallel worktrees found **19 finished** (fully merged or content-superseded) and only 2 carrying real work. (1) **`signal-api` merged to `main` (`59054ec`)** — the 4 prod-deployed-but-source-orphaned Lambdas (`newsSignals`/`newsImpactAudit`/`newsGdacsIngest`/`newsGdeltConflict`) now have source on `main`, closing the "Prod infra NOT on `main`" gap below; **merged ≠ reconciled — the deployed bytes drifted for ~5 weeks and are still the authority, so diff before changing any of the four.** Conflicts in `newsBreakingAlert` resolved as a union (`significance.js` holds BOTH the `RISK_CAP=50` rework AND the `axisForCategory` dominance fix; tests reconciled to the capped behaviour, 50/50). The V4 `thinking:{type:'disabled'}` guard was verified intact in `newsInvokeGemini` post-merge, alongside the branch's `captureIngestion` harness. (2) **P6 member-perk surfacing** cherry-picked + deployed (`80945bd`) — the load-bearing piece is a real bug: Home's trust strip counted corrections with `notes.length`, but `corrections_feed` caps that list at **5 for non-members**, so anonymous visitors were shown "5 revised conclusions logged" instead of the true total; it now reads the server's honest `total` (the *count* is public even though ledger *depth* is gated). Plus a weekly-email change-alert CTA and a Studio upsell that describes membership as allowance + history + alerts rather than compute-only. (3) **`/map`'s 4th "Economy" layer lens deleted** (`d14a2ff`) — its `econByISO` lookup missed `nameToISO`'s lowercased keys so it had **never rendered a ring**, and an always-empty exposure layer reads as "no exposure anywhere" ([[feedback_no_misinformation_fallback]]); the sibling paths (`todaySignal`, `realFlows`) lowercase correctly and were unaffected, and disruption data still reaches the page via the lede band + detail panel. A `quality/verify_pages.sh` guard that asserted the lens by grepping the bare string `"economy"` was passing **vacuously** off the panel's `tab:'economy'` deep-link — replaced with two specific guards (`daf2de9`). Cleanup: 20 worktrees + 22 branches pruned; only `main` + the `signal-api` worktree remain. Prior: **SILENT OUTPUT-TRUNCATION IN `newsSystemsAnalysis` — CEILING RAISED**. The 07-29 run went `7 generated, 3 failed` (`Unterminated string in JSON at position 10929`, Iran + Ukraine): `MAX_TOKENS` was **unset** → code default **3000**, and the graphs had outgrown it. Measured by replaying the real prompts at an 8000 ceiling — Iran **3214** tokens, Ukraine **3062**, US **3038** — i.e. the whole cohort sat in a 3038–3214 band *straddling* the limit, so which countries failed was luck (US passed that morning at 3038 and was already over). Fix = env **`MAX_TOKENS=6000`** (~87% headroom; `max_tokens` is a ceiling, not spend); re-run confirmed Iran + Ukraine now generate. **Silent class again:** caught per-country → exit 200 → CloudWatch `Errors` `0.0`, and neither `newsFreshnessMonitor` (topics only) nor `newsModelGuard` #30 (model ids only) covers it — same family as the 06-01 truncation stall. Also corrected: **`newsPostDevTo` #7 is the sole producer of the `/daily` Daily Brief** (a DeepSeek `callGrok` path entirely separate from its OpenRouter Dev.to article) — the doc had described only the Dev.to half, so nothing pointed a triager at this Lambda when `/daily` empties. Prior: **DEEPSEEK `deepseek-chat` RETIREMENT — FLEET MIGRATED TO V4**. `deepseek-chat` was retired by DeepSeek **2026-07-24**, silently breaking all 11 DeepSeek Lambdas for ~36h (most swallow the LLM error and exit 200, so CloudWatch showed 0 errors — only the freshness monitor's stale-content alert surfaced it). **Fixed 2026-07-26:** added `thinking: { type: 'disabled' }` to each function's DeepSeek request body (V4 defaults to thinking-mode → burns `max_tokens` on invisible `reasoning_content` → truncated/empty output — proven live: v4-flash without the patch clustered only 1 topic at `finish_reason=length`), then flipped `GROK_MODEL` per an intent-based split — **v4-pro** for the 3 open-analytical functions (`newsCountryIntelligence`, `newsSystemsAnalysis`, `newsEconomicImpact`), **v4-flash** for the mechanical/constrained/judge-protected/high-volume rest (`newsInvokeGemini`, `NewsProjectInvokeAgentLambda`, `newsDriftCorrector`, `newsPredictionResolver`, `newsWeeklyBrief`, `newsWeeklyMarkets`-producer, `newsPairIntelligence`, `newsImpactAudit`). Pipeline verified live end-to-end (13 topics → 39 AI items → swap). The `thinking` patch is now **ported into the repo `src/index.js` of all 10 on-main functions** (2026-07-27) so a future zip-from-repo deploy won't regress it. ✅ PPA/property project (same account) also migrated — `PPAcomputeWardProfiles-dev` + `PPAfetchMarketNews-dev` both verified on `LLM_MODEL=deepseek-v4-flash` (2026-07-27). Prior: **AGENT-REVIEW STALENESS AUDIT** — a 5-auditor parallel sweep verified the whole doc against live AWS (`AGENT_REVIEW_METHOD.md`). Fixes landed: (1) **`newsBreakingAlert` #21 is DEPLOYED + scheduled + `DRY_RUN=false`**, not "BUILT — NOT DEPLOYED" (still human-confirm-before-send); (2) **4 of 7 EventBridge *Scheduler* entries run `Asia/Tokyo`, not UTC** — `InvokeLinkedIn`/`LinkedinThreadsDaily`/`Fact` corrected (`InvokeDev` was already right); the "All times UTC" header was wrong; (3) **`newsPairIntelligence` #8 is on the ENABLED weekly rule `TriggerPairIntelligenceWeekly`**, not manual-only; (4) **Common Mistakes #7 CORS list corrected** — only `newsPolarBilling`+`newsAnalyze` own CORS in code (Function-URL CORS empty); `newsSavedItems`+`newsRecommend` do the opposite (populated Function-URL CORS, no ACAO in code) — clearing theirs would break them; (5) **a Signal-API + impact-first ingest subsystem** (`newsSignals`/`newsImpactAudit`/`newsGdacsIngest`/`newsGdeltConflict` + 6 DDB tables) is live in prod from the unmerged `signal-api` branch — now documented as "Prod infra NOT on `main`"; (6) stale counts: proxy actions ~27→~33, components ~64→67, atoms 17→19, hooks 32→34; `newsPostLinkedin` env `SOCIAL_POSTS_TABLE`→deployed `LINKEDIN_POSTS_TABLE`. Backend #10–20 + frontend routes/components/hooks mappings verified CLEAN. ~~Note to reconcile: CLAUDE.md's Dual-CORS bullet + memory `feedback_lambda_function_url_cors` still carry the old 4-function CORS list~~ — **RESOLVED (verified 2026-07-29): both already carry the corrected list** (only `newsPolarBilling`+`newsAnalyze` own CORS in code / empty Function-URL CORS; `newsSavedItems`+`newsRecommend` the opposite), re-confirmed against live `get-function-url-config`. Prior: **ANALYSIS STUDIO — VISUAL-BLOCK RELIABILITY FIX + QWEN PROVIDER** [[project_analysis_studio]]. 07-10 [`4193610`, `QWEN_AND_VISUAL_BLOCK_PLAN.md`]: a **qwen3.7-max vs deepseek-v4-pro bake-off** (same stories/lenses, shipped pipeline) exposed the real "where's the graph?" cause — DeepSeek (the member-path model) emitted the `gp-struct` block in only **1/4** runs (writes the scenario prose but skips the machine-readable block), so the 07-10 ScenarioBars/IndicatorMatrix/RippleTable rarely rendered for members; Qwen emitted 4/4. **Fix (prompt-only, `analysisPrompt.js`):** embedded a fully-filled JSON template in SYSTEM_PROMPT + a mandatory tail reminder inside each lens task (where the model finishes) + removed the easy "omit the whole block" exit for scenario/economic lenses → valid-block emission **1/4 → 8/8** live (`scratchpad/emit-test.mjs`); honesty intact — `validateStruct` still strips any figure not literally in the prose, so all 8 validated with zero fabrication. `newsAnalyze` Lambda re-synced via the **patched-deployed-zip** method (Node made deployed SYSTEM_PROMPT byte-equal to the frontend, `FE===BE` 5278 chars; deployed bytes verified: template present, omit-exit gone, `creditBalance`=0, `MAX_TOKENS`=2400; lens-task tail reminders ride in via the frontend-built `body.user`). **Bake-off verdict:** DeepSeek stays the member-path default (tighter/MECE probability bands, deeper prose, always states confidence); **Qwen3.7-max added as a faster (~36%) BYOK option** — but Alibaba's international keys are **WORKSPACE-scoped (`sk-ws-…`)** and 401 the generic `dashscope-intl` host, needing `https://<WorkspaceId>.ap-northeast-1.maas.aliyuncs.com/compatible-mode/v1` (Tokyo; verified with `ws-gl08109galyxzzh3`), so a per-provider **base-URL override** was added (`allowBaseUrlOverride` on qwen; `runChat`/`runOpenAICompat` take optional `baseUrl`→`baseUrl||provider.baseUrl`; ProviderModal optional "Endpoint URL" field; `byok.js` persists it back-compat) — live-tested end-to-end through real `runChat` (override→200, default→401). Separately (same day, prod-config only): the member path itself was switched to **`deepseek-v4-pro`** (`ANALYZE_DAILY_CAP` 50→100, `Timeout` 30s→120s), which required patching `runDeepSeek` to send `thinking:{type:'disabled'}` — v4-pro's default thinking mode otherwise burns the whole `MAX_TOKENS` budget and returns empty output — and surfaced a lesson that a timed-out synchronous Function-URL Lambda returns no-ACAO, which looks like CORS but isn't. Prior: **ANALYSIS STUDIO SURFACED + PROFESSIONAL STRUCTURE**. 07-09 [`8cf2b29`, `ANALYZE_SURFACING_PLAN.md`]: `/analyze` was discoverable from ONE nav link (~2 runs/30d = discovery gap, not quality — engine live-tested 0/3 fabrication, and a manual web truth-check found every auditor flag was a FALSE POSITIVE vs ground truth); surfaced via a per-story **"Analyze →"** pill on Home (4th control beside Summary/Predict/Trace), a Home hero value-prop line, ThreadPage **"Analyze this arc →"** (deep-links `thread.entries[0].topicId`), an `/economy` cross-link, and a new **`?stories=<id>[,…]` preselect** in `AnalysisStudio.jsx` (seeds once on topics-load, unknown ids silently dropped). 07-10 [`c429931`, `ANALYSIS_PRO_STRUCTURE_PLAN.md`]: output upgraded to ICD-203/desk-note packaging — **Key Judgments box** (BLUF bullets), the **ICD-203 probability yardstick** verbatim ("very likely (80–95%)"), **confidence as a separate justified axis**, a consolidated **`| Indicator to watch | Confirms | Kills |` table** on the scenario lens, and a fenced **`gp-struct` JSON block** (a machine-readable INDEX of the prose — never new content) parsed by new `utils/analysisStruct.js` (truncation-safe; drops scenarios whose probability digits aren't literally in the prose) → rendered by `atoms/AnalysisVisuals.jsx` (**ScenarioBars/IndicatorMatrix/RippleTable**, pure CSS). Validator + Markdown always receive the STRIPPED prose. Review caught 4 pre-deploy bugs: `newsAnalyze`'s server-pinned prompt had **DRIFTED** (missing all 06-13 upgrades — members got a weaker analyst than BYOK; now synced verbatim), gp-struct truncation at 1600 tokens (guided maxTokens→2400, Studio+eval+Lambda env), and two validator false-positives from the new structure (comparison-operator thresholds `>50%`; scenario-heading ranges — estimative look-behind 32→48; goldens added). **⚠️ prod `newsAnalyze` deploy = PROMPT-PATCH of the downloaded DEPLOYED (pre-credits 06-16) zip — the repo file carries the PARKED credits code and must NOT reach prod until `PROD_CREDITS_NEXT_STEPS.md` runs.** Prior: **SECOND PREDICTION RESOLUTION RUN + DRIFT-EMAIL PER-AXIS VIEW + FOLLOWBUTTON ON THE LEDGER** [[project_prediction_methodology_v1]] [[project_scoring_model_v2]]. The 07-08 agent-run resolution sweep scored **all 21 due triggers** [incl. the Le Pen appeal verdict + the 2 same-day-deferred triggers from the 07-07 run, cleared on recheck]. **`/track-record` now shows resolved 35 (was 15), fired 9 (was 4), Brier 0.157 (was 0.084)**; calibration buckets `0-20%`→stated ~17%/fired 9% (n11) · `20-40%`→stated ~20%/fired 9% (n11) · `60-80%`→stated ~65%/fired 54% (n13). Brier rose on purpose: run-1 was flattered by easy low-probability tails; run-2 added real high-stakes calls with genuine misses — the honest direction. The 65% bucket runs ~10pts hot (54% actual) but the curve is monotonic (9%→9%→54%). Cadence confirmed weekly agent-in-session. Prior (07-08, both already on main): **drift-alert email → per-axis view** [`0ec494f`, `SCORING_MODEL_V2_PLAN.md` §12] — `newsDriftCorrector` now embeds `currentDimensions`/`currentLead`/`currentRiskScore`/`currentRiskLevel` onto each new `DRIFT#`/`DRIFTLOG#` country note; `renderDriftEmail.js` renders a degradation ladder (currentDimensions → Level B 4-axis scorecard `HIGH · Humanitarian 82` + `⚠ N/4 elevated` → changeDimensions → Level A per-axis pills → legacy scalar); not retroactive. **FollowButton surfaced on the `/track-record` corrections ledger** [`b455ee1`, `FOLLOW_SURFACING_PLAN.md`] — the member country-follow control (previously CountryPage-only) now also renders as a sibling of the name link on each **country-scope** ledger row, the highest-intent "get the next change emailed" moment; management still lives at `/account?tab=notifications`. Deferred surfaces: CountryWhatChanged band (redundant), ThreadPage (multi-country ambiguity), countries-index (nesting/grid). Prior: **FIRST PREDICTION RESOLUTION RUN DONE + G6 SPORTS-SCOPE GATE LIVE** [[project_prediction_methodology_v1]]. The accountability record is no longer empty: the first agent-run resolution loop (`predictions/V1_RESOLUTION_RUNBOOK.md`) scored **16 of 21 due triggers** — 3 parallel blind web-grounded verifiers → 2nd independent blind pass on every `fired` → operator ~10% spot-check → dry-run → `resolve-v1-write.js --commit`. **`/track-record` now shows Brier 0.084**, resolved 15 (fired 4 · not_fired 11) + 1 unclear, calibration buckets 0-20%→0% (n6) · 20-40%→0% (n4) · 60-80%→80% (n5) [caveat: n tiny, 11/15 were low-p tails correctly not firing = easy points]; 5 triggers deferred (window still open on run day — Le Pen ruling ×3, INDOPACOM, Andersen). Deadlines arrived earlier than the ~07-11 estimate (short-horizon triggers). **New capture gate G6** [`NewsProjectInvokeAgentLambda/src/lib.js`, deployed to `-dev` + deployed-bytes verified; source `91e069e` + deploy `73e490d`]: drops **pure sporting-result** triggers — a match/tournament outcome or on-field participation carries no geopolitical/economic/institutional signal (the remit). Rule = drop only when BOTH a sporting-competition **context** regex AND a **result/participation** regex match, so sport-**adjacent** governance triggers survive (no result verb: "Belgium files a FIFA protest", "CAS grants an interim measure" pass). **Corpus-validated: across all 1038 live v1 triggers G6 drops exactly the 2 Trump–FIFA/Balogun box-score triggers, 0 false positives**; `node --test` 21/21. NOT retroactive (already-published sport triggers stay scored); scope decision was fix-forward at the trigger layer, NOT an ingest ban. Methodology disclosure live on `/track-record`. Cadence = weekly agent-in-session. Prior: **MEMBER AUTO-FIX GATING LIVE** [`MEMBER_GATING_PLAN.md` — merged to main `f3e0f4d`, prod-verified]: membership now buys the **depth + alerts** of the self-correcting-analysis layer, while every analysis page + the *latest* correction + the whole track record stay **free** — **data-cap, never a hard block** (anon always 200 → the 2026-04-22 anon-guard regression can't recur). **P1 depth-cap:** `newsSensitiveData-dev` `resolveTier` (best-effort Firebase-JWT → `GlobalPerspectiveUserTable.tier`) + pure `capForTier` (`src/lib.js`, `node --test`) → `country_history` caps `driftNotes`→newest 1 + `driftNotesTotal`/`driftNotesGated`; `corrections_feed` caps→newest 5 + `total`/`gated`; members get the full chain [prod: anon Ukraine 1/6 gated, ledger 5/26 gated, 51 snapshots uncapped]. **P2** lock affordances [`CountryWhatChanged` + `/track-record` — "🔒 N earlier — Join", reads server `gated`/`total`, no client tier check]. **P3 permanent archive:** `newsDriftCorrector` now writes each note TWICE via `putDriftNote` — `DRIFT#<date>` (60d TTL) + **`DRIFTLOG#<date>` (NO TTL)** so member history outlives 60d; `country_history` unions archive+live via pure `dedupeByAsOf` [tests 9/9; empty archive ⇒ no-op; `begins_with('DRIFT#')` excludes `DRIFTLOG#` → no double-count]. **P5 follow-country change-alerts** [the member perk]: `newsRecommend` `followedCountries` (String Set, atomic ADD/DELETE) + `driftOptIn`; `follow_country`/`unfollow_country` = signed-in AND member-tier [`getTier` fail-closed → `403 members_only`; **new IAM `ReadTier` GetItem on `GlobalPerspectiveUserTable` added to `newsRecommend-ddb`** — required or nobody can follow]; `handleUnsubscribe` kind=`drift`; `newsEmailSender` `mode:'drift_alert'` (`runDrift` + `renderDriftEmail.js`, mirrors `runBreaking` emailedAt idempotency); frontend `FollowButton` [member 🔔 toggle / non-member locked `/membership` CTA] + `/account` "Country change-alerts" list. **⚠️ drift email OFF by design:** `TriggerDriftEmailSend` created **DISABLED** [`cron(40 7 * * ? *)` → `{mode:drift_alert}`; sender is `DRY_RUN=false`=live → send-to-self smoke test before enabling; 0 drift subscribers today]. **P4** membership copy repositioned across 6 spots [old "buys compute, not access" → "reading free; membership adds depth"]. Deploy: no function needs bundled `node_modules` (runtime `@aws-sdk` + node `crypto`); frontend bundle `index-DRIay5Lv.js`; single ACAO verified. [[project_member_gating]]. Prior: **SITE ORIENTATION SHIPPED** [`SITE_ORIENTATION_PLAN.md` — deploys `729dc9e` + `72baef1`, prod-verified]: the diagnosis was a feature-surplus with an **orientation gap**, not a missing landing page — so NO new pages. (P1) Home masthead now states the accountability value prop [links `/track-record`] + an **honest-data trust strip** above `SubscribeCard` [3 stat cards from `useTrackRecord`/`useCorrectionsFeed`/today's sources; each card omitted when its number is absent; live 815/26/34]; (P2) the 9-item nav is **grouped** [Briefings · Intelligence · Markets/Analysis · Accountability] with hairline dividers + a per-item `title` tooltip [canonical page one-liners in the plan doc]; (P3) trust-chain cross-links [ThreadForecast→`/track-record`, TrackRecord→`/weekly` live boards, CountryWhatChanged→`/track-record`]; (P4) `index.html` meta/OG/FAQ-schema carry the accountability positioning + **fixed the noscript `/pricing` dead link** [deleted route — crawlers 404'd] → `/track-record`+`/membership`; the Worker `/` bot branch [`renderRootPage()`, static, 24h cache] is **LIVE — deployed via `wrangler deploy` [version `8b73c9c7`, drift-checked, dashboard routes preserved; recipe in the plan §P4]**; (P5) pairs cleanup **CANCELLED on evidence**: `pair_analyses_list` is live-consumed by the `/map` arc overlays [`WorldMapV2`→`usePairAnalyses`] — the old "unconsumed" note here was stale and is corrected at Lambda #8. Prior: **PREDICTION METHODOLOGY v1 LIVE** — the forecast pipeline was rebuilt + **deployed to prod 2026-07-04 (the era-cut date)**. A 50-trigger resolution pilot found the legacy prediction log was structurally unscoreable — **28% retrodictions** [trigger `deadline ≤ generatedAt` → automatic wins → silent Brier inflation], plus false-premise + date-artifact triggers — so the decision was **clean-start**: don't score the legacy backlog, rebuild capture, score only `methodologyVersion:1` records going forward. New `NewsProjectInvokeAgentLambda/src/lib.js` [pure, `node --test` 16/16] holds the **capture gates G1–G5** [date-valid · forward-looking · ≤180d horizon · falsifiability lint · `FACTS#` premise-check]; `logPredictionSnapshot` runs every trigger through `buildGatedScenarios` → structured `{text,deadline}` triggers + a `capture.dropped` report + `methodologyVersion:1` stamp. Generation is now **grounded**: verified `FACTS#` leader premises + the story's own dated **arc digest** + optional Brave web context. **Verified live in prod** [27 v1 snapshots, gates firing: G4 caught relative-window slips, 0 retrodictions]. A real integration bug was caught pre-declare [`generatedDate` is a human string "July 4, 2026", not ISO — slicing it broke `addDays`; fixed to UTC-today]. Merged to main [`6a90b14`] + pushed. **Phase 3 SHIPPED 2026-07-05** [`8f94806` source + `c76fbd9` deploy]: **`/track-record` rebuilt into the "Accountability" hub** [kept the URL — reversible] — (1) era-cut forecast scorecard [scores only `methodologyVersion>=1`; `prediction_track_record` now returns `eraCutFrom`/`legacyPredictionsExcluded`; live = 2418 legacy excluded, resolved 0, honest-empty], (2) **corrections ledger** [new `corrections_feed` action Scans DRIFT# across all countries+threads; the living-analysis loop's site-level home; live = 26 notes], (3) published methodology incl. the pre-v1 exclusion disclosure. **Phase 4 SHIPPED 2026-07-06** [`d53e40a` source + `6192fcb` deploy]: **"Living forecast" board on ThreadPage** — the new `prediction_snapshot` action returns a thread's newest v1 `PRED#` snapshot [scenarios + dated triggers]; `ThreadForecast` renders it at the top of the Arc Intelligence rail as a trigger checklist [`☐` open · `✓` fired · `~~✗~~` not_fired · `⌛` awaiting], pending today and striking/checking as verdicts land. Wiring proven: 55/145 active threads carry a v1 forecast. **All four phases now shipped**; Phase 2 tooling ready [`predictions/resolve-v1-*.js`; first live resolution run ~2026-07-11]. Plan `PREDICTION_METHODOLOGY_V1_PLAN.md` + worked example `PREDICTION_V1_EXAMPLE.md`; [[project_prediction_methodology_v1]]. Prior: **EMAIL DELIVERY LIVE** — the send path both products were missing is now sending real mail: **`newsEmailSender`** Lambda [#29, `event.mode` weekly|breaking] off the shared Resend seam + `GlobalPerspectiveUserPrefs`. Domain `globalperspective.net` **verified in Resend 2026-07-04** [Cloudflare auto-config: DKIM/SPF/MX/DMARC live], `EMAIL_SEND_DRY_RUN=false`, from `brief@globalperspective.net`, `TEST_RECIPIENT` cleared — a **real weekly send to the first subscriber confirmed** [`sent:1, errors:[]`]. Both rules ENABLED [`TriggerWeeklyEmailSend` Sun 14:00 UTC, `TriggerBreakingEmailSend` rate(15m)]; idempotency markers + 48h breaking-freshness gate; public one-click **unsubscribe** on `newsRecommend` [GetItem-by-uid, no new IAM]; 28/28 hermetic tests. **`SubscribeCard`** LIVE on Home + `/weekly-brief` [digest] + `/breaking` [breaking] — reuses the `digestOptIn`/`breakingOptIn` opt-ins, no backend change. See `EMAIL_SENDER_PLAN.md`, [[project_email_sender]]. Prior: **RISK TIERS MIGRATION COMPLETE** [`RISK_TIERS_PLAN.md` P1+P2+P3, prod-verified] — new `src/utils/riskTiers.js` is the single source of truth for score→tier on the **canonical 25/50/75 bands**, collapsing **four** divergent band definitions + fixing the moderate tier [was always orange "elevated"; now amber]. Current-state risk displays across ThreadPage/CountryPage/WorldMapV2 now lead with the **tier word** + demote the raw score to fine print [rule: *state = tier · change = audit numbers*]; `/weekly` gains a **LEAD + DEVELOPING front-page hierarchy** above the category river [tier-qualified, drift-delta line, hides in work-mode, honest-empty]. See the Design-Tokens + `WeeklyPage` notes below. Prior: **LIVING-ANALYSIS LOOP LIVE + browser-verified** — self-correcting country/thread analysis. New `newsDriftCorrector` Lambda [#26, DeepSeek, daily `TriggerDriftCorrector` cron 07:20 UTC] detects when a read's *conclusion* moved [risk level / |Δscore|≥8 / trajectory — gated on the conclusion, NOT the headline] and grounds the "why" in a **real cited archive event** [numbered-event prompt → single trigger or honest "no single driver"; never invents], writing `COUNTRY#/DRIFT#<date>` + `THREAD#/DRIFT#<date>` notes [never overwrites, 60d TTL]. It **backfills every ungrounded move** in its 10-day window [`findAllDrifts`], so pre-live history / a missed cron day self-heals to full coverage — last-week backtest = **11/11 moves grounded [100%]**. `newsCountryIntelligence` feeds recent notes forward as a low-authority "RECENT CORRECTIONS" block [1b.5]; `newsThreadAnalysis` now writes `THREAD_HISTORY#<date>` snapshots. Surfaced as the **"What changed" band + expandable correction-history chain** on CountryPage [`atoms/CountryWhatChanged`, served via `country_history.driftNotes[]`] and a "What changed" block on ThreadPage [`thread_analysis.driftNote`]. Playwright-verified live [Ukraine/Japan/Russia]. Prior: **Weekly Markets consolidated into `/economy`** — the standalone `/weekly-markets` page became a "This week" mode behind a Today/This-week toggle [`?view=week`], rendered by the new `WeeklyMarketsView`; `/weekly-markets` is now a redirect permalink; the "Markets" nav item was removed. Merged to main `41f2b5a`, not yet built/deployed. Prior: **narrative-page layout cleanup** LIVE — ThreadPage collapsed to a single tab system [right "Arc Intelligence" rail de-tabbed → stacked synthesis column, actors/economy de-duplicated to the center tabs]; CountryPage's `SystemsGraph` causal web promoted out of the overloaded right rail into a full-width center **Causal Web** tab. Prior: **Weekly Markets Report** LIVE — price-first `/weekly-markets` "what moved & why" wrap via the new `newsWeeklyMarkets` Lambda [#25] + `weekly_markets` serve action, first report published & browser-tested; deterministic moves + 3 honesty-layered grounding tiers; a markets-history gap [06-06..06-21] was backfilled (delete-null-rows → seed_history), also fixing `/economy` sparklines. Prior: breaking-alert **web surface** LIVE — `/breaking` feed + `/breaking/:id` detail + Home/Map `BreakingStrip`, bell rewired off the thread page; `newsRecommend` `get_alert` + enriched `list_alerts` deployed. Prior: `src/tokens.js` single source for risk/category colors [P2a]; P0/P1 + systems-graph product-improvement work deployed live; Stooq→Yahoo markets migration, Polar membership LIVE + `newsAnalyze`/`newsPolarBilling`, `/membership` route, `newsPostDevTo` TZ correction)
